# vim: set expandtab ts=8 sw=8:

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:Ubuntu 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier Mod1

# start a terminal
set $terminal i3-sensible-terminal
bindsym $mod+t exec $terminal
bindsym $mod+Return exec $terminal

# kill focused window
bindsym $mod+Shift+q kill

# Rofi
set $dmenu "rofi -combi-modi window,drun,ssh,emoji -show combi -modi combi"
bindsym $mod+d exec --no-startup-id $dmenu
bindsym $mod+space exec --no-startup-id $dmenu
bindsym $mod+Shift+s exec screen-layout

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# move to other display
bindsym $mod+Shift+m move workspace to output right
bindsym $mod+Shift+n move workspace to output left
bindsym $mod+Shift+comma move workspace to output down

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set     $w0     0...
set     $w1     1...
set     $w2     2...
set     $w3     3...
set     $w4     4...
set     $w5     5...
set     $w6     6...
set     $w7     7...
set     $w8     8...
set     $w9     9...
set     $w10    10...
set     $w11    11...
set     $w12    12...

set     $key0   dead_circumflex
set     $key11  ssharp
set     $key12  dead_acute


# switch to workspace
bindsym $mod+$key0 workspace $w0
bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
bindsym $mod+0 workspace $w10
bindsym $mod+$key11 workspace $w11
bindsym $mod+$key12 workspace $w12

# move focused container to workspace
bindsym $mod+Shift+$key0 move container to workspace $w0
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+0 move container to workspace $w10
bindsym $mod+Shift+$key11 move container to workspace $w11
bindsym $mod+Shift+$key12 move container to workspace $w12

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# quiet mode (kill dunst)
bindsym $mod+Shift+x exec "killall dunst; dunst -transparency 100"
bindsym $mod+Shift+a exec "killall dunst; dunst"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
bindsym $mod+x [urgent=latest] focus

set $black      #000000

# Solarized Colors
set $base0     #839496
set $base03    #002b36
set $base02    #073642
set $base01    #586e75
set $base00    #657b83
set $base1     #93a1a1
set $base2     #eee8d5
set $base3     #fdf6e3
set $yellow    #b58900
set $orange    #cb4b16
set $red       #dc322f
set $magenta   #d33682
set $violet    #6c71c4
set $blue      #268bd2
set $cyan      #2aa198
set $green     #859900


# colors                    border      background  text    indicator   child_border
client.focused              $blue       $blue       $base3  $green
client.focused_inactive     $blue       $base02     $base1
client.unfocused            $blue       $base02     $base1  $blue
client.urgent               $red        $red        $base3    $black

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
        position bottom
        tray_output primary

        colors {
                background              $base03
                statusline              $blue
                separator               $red

                #                       border      background      text
                focused_workspace       $blue       $blue           $base3
                active_workspace        $blue       $base02         $base1
                inactive_workspace      $base02     $base02         $base1
                urgent_workspace        $red        $red            $base3
        }
}

# applications
bindsym $mod+n exec pcmanfm
bindsym $mod+g exec google-chrome-stable
bindsym $mod+Shift+g exec firefox
bindsym $mod+c exec gnome-calculator
bindsym $mod+p exec phpstorm
bindsym $mod+o exec thunderbird

# assign applications to workspaces
assign [class="Skype"] $w2
assign [class="Slack"] $w2
assign [class="Telegram"] $w2
assign [class="Rocket.Chat"] $w2
assign [class="Thunderbird"] $w10
# Todoist
assign [instance="crx_bgjohebimpjdhhocbknplfelpmdhifhd"] $w10

# extra rules for some windows
for_window [class="Gnome-calculator"] floating enable
for_window [instance="crx_gaedmjdfmmahhbjefcbgaolhhanlaolb"] floating enable
for_window [class="Spotify"] move to workspace $w12

# Lockscreen
set $lockCmd ~/.xlock

# bindsym XF86Launch1 exec $lockCmd
# bindsym XF86Sleep exec $lockCmd
# bindsym XF86ScreenSaver exec "xset dpms force off && $lockCmd"

bindsym Control+Mod1+l exec $lockCmd
bindsym Control+Mod1+k exec "systemctl susped && $lockCmd"

# Volume
bindsym XF86AudioRaiseVolume exec amixer -q set Master 5%+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 5%- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle

# Brightness
#bindsym XF86MonBrightnessUp exec xbacklight -inc 5
bindsym XF86MonBrightnessUp exec sudo backlight inc
#bindsym XF86MonBrightnessDown exec xbacklight -dec 5
bindsym XF86MonBrightnessDown exec sudo backlight dec

# Media Keys
bindsym XF86AudioPlay exec /usr/local/bin/mediakeys playpause
bindsym XF86LaunchA exec /usr/local/bin/mediakeys playpause
bindsym XF86AudioStop exec /usr/local/bin/mediakeys stop
bindsym XF86AudioPrev exec /usr/local/bin/mediakeys prev
bindsym XF86AudioNext exec /usr/local/bin/mediakeys next
bindsym XF86Explorer exec /usr/local/bin/mediakeys next

# Screenshot
bindsym --release Print exec --no-startup-id flameshot gui -p /home/markus/Pictures/flameshot/
bindsym --release Ctrl+Print exec --no-startup-id scrot '/home/markus/Pictures/Screenshot from %Y-%m-%d %T.png'$ && pcmanfm /home/markus/Pictures
bindsym --release Shift+Print exec --no-startup-id scrot -s '/home/markus/Pictures/Screenshot from %Y-%m-%d %T.png'$ && pcmanfm /home/markus/Pictures

# set desktop background
exec_always feh --auto-zoom --bg-fill ~/Pictures/Wallpaper/jeremy-weber-H1SOELwNtTw-unsplash.jpg

# Lockscreen
exec xautolock -time 10 -detectsleep -locker ~/.xlock
exec xss-lock -- ~/.xlock 
exec xscreensaver -no-splash

# Autostart
exec_always parcellite
exec dropbox
exec keepassxc
exec nextcloud
exec redshift-gtk -l 53.551086:9.993682
exec dunst
exec /opt/google/chrome/google-chrome --profile-directory=Default --app-id=bgjohebimpjdhhocbknplfelpmdhifhd # Todoist


# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# # Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Show Menu to restart, shutdown etc.
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $lockCmd, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $lockCmd && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $lockCmd && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Shift+e mode "$mode_system"
